{
  "name": "netfiles",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./src --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files  && cp -r src/views dist/ && cp -r src/public dist/",
    "start": "npm run build && node dist/app.js"
  },
  "author": "Ronald Dennison <ronniedz@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "express": "^4.16.3",
    "http-proxy-middleware": "^0.18.0",
    "morgan": "^1.9.0",
    "multiparty": "^4.1.3",
    "pug": "^2.0.3",
    "rimraf": "^2.6.2"
  }
}
